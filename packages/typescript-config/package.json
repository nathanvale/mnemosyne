{
  "name": "@studio/typescript-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./base.json": "./base.json",
    "./nextjs.json": "./nextjs.json",
    "./library.json": "./library.json",
    "./test.json": "./test.json",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "echo 'TypeScript config - no build needed'",
    "lint": "echo 'TypeScript config - no linting needed'",
    "format:check": "prettier --check *.json",
    "test": "echo 'TypeScript config - no tests needed'",
    "type-check": "echo 'TypeScript config - no TypeScript to check'",
    "clean": "echo 'TypeScript config - no clean needed'"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
