{
  "name": "@studio/prettier-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "echo 'Prettier config - no build needed'",
    "lint": "echo 'Prettier config - no linting needed'",
    "format:check": "prettier --check *.js",
    "test": "echo 'Prettier config - no tests needed'",
    "type-check": "echo 'Prettier config - no TypeScript to check'",
    "clean": "echo 'Prettier config - no clean needed'"
  },
  "peerDependencies": {
    "prettier": "^3.0.0"
  }
}
